// Generated by CoffeeScript 1.6.1
(function() {
  var Event, Events, IDENTICAL, NUM, RATIO, Router, SERIES, TIME, averageLine, averageLine2, averageLine3, avg, avgTotal, calcSizes, chartHeight, chartWidth, data, equalLabel, events, femaleLabel, getHeight, h, initVis, legendBorder, legendBorder2, nicePercent, padding, posScale, router, scaling, sortFunc, sortMode, sortingChanged, storage, svg, totalNumSpeakers, updatePositions, updateScales, updateVis, valueScale, verticalLabels, verticalLabelsData, w, widthScale, xAxis, xAxisLegend,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    _this = this;

  storage = Tabletop.init({
    key: '0AkTjJmB1VuOXdGV1NkRnU1Y4d2pJeHd6Y2wybTZ1ZVE',
    wait: true,
    parseNumbers: true,
    simpleSheet: true,
    singleton: true,
    debug: false
  });

  RATIO = "RATIO";

  TIME = "TIME";

  SERIES = "SERIES";

  NUM = "NUM";

  IDENTICAL = "IDENTICAL";

  sortMode = null;

  scaling = null;

  router = null;

  Event = (function(_super) {

    __extends(Event, _super);

    function Event() {
      return Event.__super__.constructor.apply(this, arguments);
    }

    Event.prototype.initialize = function(o) {
      this.set("series", o["conferenceseries"]);
      this.set("date", o.year + "-" + o.month);
      this.set("numMale", o.nummale);
      this.set("numFemale", o.numfemale);
      this.set("numTotal", o.numfemale + o.nummale);
      return this.set("ratioFemale", o.numfemale / (o.numfemale + o.nummale));
    };

    return Event;

  })(Backbone.Model);

  Events = (function(_super) {

    __extends(Events, _super);

    function Events() {
      return Events.__super__.constructor.apply(this, arguments);
    }

    Events.prototype.tabletop = {
      instance: storage,
      sheet: 'collection'
    };

    Events.prototype.sync = Backbone.tabletopSync;

    Events.prototype.model = Event;

    return Events;

  })(Backbone.Collection);

  Router = (function(_super) {

    __extends(Router, _super);

    function Router() {
      var _this = this;
      this.path = function(x) {
        return Router.prototype.path.apply(_this, arguments);
      };
      return Router.__super__.constructor.apply(this, arguments);
    }

    Router.prototype.routes = {
      "*args": "path"
    };

    Router.prototype.path = function(x) {
      sortMode = x.split("/")[0] || RATIO;
      scaling = x.split("/")[1] || NUM;
      router.navigate("" + sortMode + "/" + scaling, {
        trigger: false
      });
      return updateVis();
    };

    return Router;

  })(Backbone.Router);

  w = 0;

  h = 0;

  padding = {};

  chartHeight = 0;

  chartWidth = 0;

  totalNumSpeakers = 0;

  avg = 0;

  avgTotal = 0;

  widthScale = null;

  valueScale = null;

  data = null;

  getHeight = null;

  posScale = null;

  events = null;

  averageLine = null;

  averageLine2 = null;

  averageLine3 = null;

  xAxis = null;

  xAxisLegend = null;

  legendBorder = null;

  legendBorder2 = null;

  equalLabel = null;

  femaleLabel = null;

  verticalLabels = null;

  verticalLabelsData = [];

  svg = null;

  sortingChanged = true;

  nicePercent = function(x) {
    return "" + ((x * 100).toFixed(1)) + "%";
  };

  calcSizes = function() {
    w = $(".chart").width();
    h = $(".chart").height();
    padding = {
      top: 35,
      right: 20,
      bottom: 160,
      left: 20
    };
    chartHeight = h - padding.top - padding.bottom;
    return chartWidth = w - padding.left - padding.right;
  };

  updateScales = function() {
    calcSizes();
    valueScale.range([0, chartWidth]);
    xAxis.scale(valueScale).ticks(w > 600 ? 10 : 5);
    xAxisLegend.call(xAxis);
    if (legendBorder != null) {
      legendBorder.attr({
        width: w + 6
      });
    }
    if (legendBorder2 != null) {
      legendBorder2.attr({
        width: w + 6
      });
    }
    if (femaleLabel != null) {
      femaleLabel.attr({
        x: 20 + valueScale(1)
      });
    }
    if (equalLabel != null) {
      equalLabel.attr({
        x: valueScale(.5)
      }).style({
        opacity: w > 400 ? 1 : 0
      });
    }
    svg.selectAll(".tick").transition().duration(500).attr({
      transform: function(d) {
        return "translate(" + (valueScale(d)) + ", 0)";
      }
    }).style({
      opacity: function(_, i) {
        if (w > 600 || (i % 2) === 0) {
          return 1;
        } else {
          return 0;
        }
      }
    });
    averageLine.attr({
      transform: "translate(" + (valueScale(avg)) + ", " + (chartHeight + 150) + ")"
    });
    averageLine2.attr({
      transform: "translate(" + (valueScale(avgTotal)) + ", " + (chartHeight + 110) + ")"
    });
    averageLine3.attr({
      transform: "translate(" + (valueScale(.2307)) + ", " + (chartHeight + 70) + ")"
    });
    if (scaling === NUM) {
      posScale = d3.scale.linear().domain([0, totalNumSpeakers]).range([0, chartHeight - data.length - 20]);
      return getHeight = function(d) {
        return posScale(d.numTotal);
      };
    } else {
      posScale = d3.scale.linear().domain([0, data.length]).range([0, chartHeight - data.length - 20]);
      return getHeight = function(d) {
        return posScale(1);
      };
    }
  };

  sortFunc = function(a, b) {
    switch (sortMode) {
      case RATIO:
        if (a.ratioFemale > b.ratioFemale) {
          return 1;
        }
        if (a.ratioFemale < b.ratioFemale) {
          return -1;
        }
        if (a.series > b.series) {
          return 1;
        }
        if (a.series < b.series) {
          return -1;
        }
        if (a.date > b.date) {
          return 1;
        }
        if (a.date < b.date) {
          return -1;
        }
        return 0;
      case TIME:
        if (a.date > b.date) {
          return 1;
        }
        if (a.date < b.date) {
          return -1;
        }
        if (a.event > b.event) {
          return 1;
        }
        if (a.event < b.event) {
          return -1;
        }
        return 0;
      case SERIES:
        if (a.series > b.series) {
          return 1;
        }
        if (a.series < b.series) {
          return -1;
        }
        if (a.date > b.date) {
          return 1;
        }
        if (a.date < b.date) {
          return -1;
        }
        if (a.event > b.event) {
          return 1;
        }
        if (a.event < b.event) {
          return -1;
        }
        return 0;
      case NUM:
        if (a.numTotal > b.numTotal) {
          return 1;
        }
        if (a.numTotal < b.numTotal) {
          return -1;
        }
        if (a.date > b.date) {
          return 1;
        }
        if (a.date < b.date) {
          return -1;
        }
        if (a.event > b.event) {
          return 1;
        }
        if (a.event < b.event) {
          return -1;
        }
        return 0;
    }
  };

  updatePositions = function() {
    var d, lastValue, y, _i, _len, _results;
    y = 0;
    verticalLabelsData = [];
    lastValue = null;
    _results = [];
    for (_i = 0, _len = data.length; _i < _len; _i++) {
      d = data[_i];
      d.x = valueScale(d.ratioFemale);
      d.y = y + 10;
      d.height = getHeight(d);
      d.labelVisible = d.height > 10;
      y += 1 + d.height;
      if (verticalLabelsData.length) {
        verticalLabelsData[verticalLabelsData.length - 1].height = d.y - verticalLabelsData[verticalLabelsData.length - 1].y;
      }
      switch (sortMode) {
        case TIME:
          if (d.year !== lastValue) {
            verticalLabelsData.push({
              label: d.year,
              y: d.y
            });
            _results.push(lastValue = d.year);
          } else {
            _results.push(void 0);
          }
          break;
        case SERIES:
          if (d.series !== lastValue) {
            verticalLabelsData.push({
              label: d.series,
              y: d.y
            });
            _results.push(lastValue = d.series);
          } else {
            _results.push(void 0);
          }
          break;
        default:
          _results.push(void 0);
      }
    }
    return _results;
  };

  updateVis = function() {
    var ani, enter, vl;
    updateScales();
    data = data.sort(sortFunc);
    updatePositions();
    d3.select("#scaling").selectAll("a").classed("active", function() {
      return d3.select(this).attr("data-value") === scaling;
    });
    d3.select("#sortMode").selectAll("a").classed("active", function() {
      return d3.select(this).attr("data-value") === sortMode;
    });
    ani = events.transition().duration(500).delay(function(d, i) {
      return d.y + d.x;
    });
    ani.attr({
      transform: function(d) {
        return "translate(" + d.x + ", " + d.y + ")";
      }
    });
    ani.selectAll(".bg").attr({
      height: function(d) {
        return d.height;
      }
    });
    ani.selectAll(".female").attr({
      height: function(d) {
        return d.height;
      }
    });
    ani.selectAll(".male").attr({
      height: function(d) {
        return d.height;
      }
    });
    ani.selectAll(".male").attr({
      height: function(d) {
        return d.height;
      }
    });
    ani.selectAll("text").attr({
      opacity: function(d) {
        if (d.labelVisible) {
          return 1;
        } else {
          return 0;
        }
      }
    });
    verticalLabels.attr({
      transform: "translate(" + (w - 220) + ", 0)"
    });
    vl = verticalLabels.selectAll("g").data(verticalLabelsData, function(d) {
      return d.label;
    });
    enter = vl.enter().append("g").classed("verticalLabel", true);
    enter.attr({
      "transform": function(d) {
        return "translate(0, " + (h / 2 + 2 * (d.y - h / 2)) + ")";
      }
    }).style({
      opacity: 0
    });
    enter.append("text").text(function(d) {
      return d.label;
    }).attr({
      "transform": "translate(-3, 12)",
      "text-anchor": "end"
    });
    enter.append("line").attr({
      x1: -2000,
      x2: 0,
      y1: 0,
      y2: 0
    }).attr({
      "stroke-dasharray": "1,1"
    });
    vl.selectAll("text").text(function(d) {
      if (d.height > 15) {
        return d.label;
      } else {
        return "";
      }
    });
    vl.transition().duration(500).delay(sortingChanged ? 1500 : function(d) {
      return d.y;
    }).attr({
      transform: function(d) {
        return "translate(0, " + d.y + ")";
      }
    }).style({
      opacity: 1
    });
    return vl.exit().transition().duration(500).attr({
      transform: function(d) {
        return "translate(0, " + (h / 2 + 2 * (d.y - h / 2)) + ")";
      }
    }).style({
      opacity: 0
    }).remove();
  };

  initVis = function() {
    var container, enter, textGroup;
    $(".chart").watch(["width"], updateVis);
    data = _this.eventsData.toJSON();
    totalNumSpeakers = d3.sum(data, function(x) {
      return x.numTotal;
    });
    avg = d3.mean(data, function(x) {
      return x.ratioFemale;
    });
    avgTotal = (d3.sum(data, function(x) {
      return x.numFemale;
    })) / totalNumSpeakers;
    svg = d3.select(".chart").append("svg").attr({
      width: "100%",
      height: "100%"
    });
    calcSizes();
    widthScale = d3.scale.linear().domain([0, 1]).range([0, 20]);
    valueScale = d3.scale.linear().domain([0, 1]).range([0, chartWidth]);
    container = svg.append("g").attr({
      "transform": "translate(" + padding.left + ", " + padding.top + ")"
    });
    xAxis = d3.svg.axis().scale(valueScale).tickSize(chartHeight - 3).tickPadding(13).ticks(10).orient("bottom").tickFormat(function(d, i) {
      return "" + (Math.floor(d * 100)) + "%";
    });
    d3.select("#average1").text(nicePercent(avg));
    averageLine = container.append("g").classed("averageLine", true).attr({
      transform: "translate(" + (valueScale(avg)) + ", " + (chartHeight + 150) + ")"
    });
    averageLine.append("line").attr({
      x1: 0,
      x2: 0,
      y1: 0,
      y2: -130
    });
    averageLine.append("text").attr({
      x: 5
    }).text("Average proportion of female speakers per conference");
    averageLine.append("text").classed("percentage", true).attr({
      x: 5,
      y: -15
    }).text(nicePercent(avg));
    d3.select("#average2").text(nicePercent(avgTotal));
    averageLine2 = container.append("g").classed("averageLine", true).attr({
      transform: "translate(" + (valueScale(avgTotal)) + ", " + (chartHeight + 110) + ")"
    });
    averageLine2.append("line").attr({
      x1: 0,
      x2: 0,
      y1: 0,
      y2: -90
    });
    averageLine2.append("text").attr({
      x: 5
    }).text("Overall proportion of female speakers");
    averageLine2.append("text").classed("percentage", true).attr({
      x: 5,
      y: -15
    }).text(nicePercent(avgTotal));
    averageLine3 = container.append("g").classed("averageLine target", true).attr({
      transform: "translate(" + (valueScale(.2307)) + ", " + (chartHeight + 70) + ")"
    });
    averageLine3.append("line").attr({
      x1: 0,
      x2: 0,
      y1: 0,
      y2: -50
    });
    averageLine3.append("text").attr({
      x: 5
    }).text("Proportion of women in datavisualization");
    averageLine3.append("text").classed("percentage", true).attr({
      x: 5,
      y: -15
    }).text(nicePercent(.2307));
    legendBorder = container.append("rect").attr({
      x: -padding.left - 3,
      y: -padding.top - 1,
      width: w + 6,
      height: padding.top - 5
    }).classed("legendBorder", true);
    legendBorder2 = container.append("rect").attr({
      x: -padding.left - 3,
      y: chartHeight - 1,
      width: w + 6,
      height: 30
    }).classed("legendBorder", true);
    verticalLabels = container.append("g");
    xAxisLegend = container.append("g").classed({
      "axisLegend x": true
    }).call(xAxis).selectAll("line").attr({
      "stroke-dasharray": "1,4"
    });
    xAxisLegend.selectAll(".tick").style("opacity", 0);
    container.append("text").attr({
      x: -20 + valueScale(0),
      y: -15
    }).classed("male legend", true).text("Only male speakers");
    femaleLabel = container.append("text").attr({
      x: 20 + valueScale(1),
      y: -15,
      "text-anchor": "end"
    }).classed("female legend", true).text("Only female speakers");
    equalLabel = container.append("text").attr({
      x: valueScale(.5),
      y: -15,
      "text-anchor": "middle"
    }).classed("legend", true).text("Equal mixture");
    events = container.append("g").selectAll("g.event").data(data, function(d) {
      return d.event;
    });
    enter = events.enter().append("g").classed({
      "event": true
    }).attr({
      "title": function(d) {
        return "<table>\n	\n	<tr >\n		<td class=\"titleCell\" rowspan=\"2\">\n			<div class=\"year\">" + d.year + "</div>\n			<div class=\"title\">" + d.event + "</div>\n		</td>\n		<td class=\"numbers\">\n			<span class=\"numFemale\">" + d.numFemale + "</span>\n		</td>\n		<td class=\"labels\">\n			female speaker" + (d.numFemale !== 1 ? "s" : "") + "\n		</td>\n	</tr>\n	<tr>\n		<td class=\"numbers\">\n			<span class=\"numMale\">" + d.numMale + "</span>\n		</td>\n		<td class=\"labels\">\n			male speaker" + (d.numMale !== 1 ? "s" : "") + "\n		</td>\n	</tr>\n</table>";
      }
    });
    enter.attr({
      transform: function(d) {
        return "translate(-200, " + d.y + ")";
      }
    });
    enter.append("rect").classed("bg", true).attr({
      x: -widthScale(.5),
      y: 0,
      width: function(d) {
        return widthScale(1);
      }
    });
    enter.append("rect").classed("male", true).attr({
      x: function(d) {
        return -widthScale(.5) - 1;
      },
      y: 0,
      width: function(d) {
        return widthScale(1 - d.ratioFemale);
      }
    });
    enter.append("rect").classed("female", true).attr({
      x: function(d) {
        return -widthScale(.5) + widthScale(1 - d.ratioFemale);
      },
      y: 0,
      width: function(d) {
        return widthScale(d.ratioFemale);
      }
    });
    textGroup = enter.append("g").classed("label", true).attr({
      transform: "translate(" + (widthScale(.5) + 5) + ", " + 11 + ")"
    });
    textGroup.append("text").text(function(d) {
      return d.event;
    }).style({
      "stroke": "#FFF",
      "stroke-width": "2px"
    });
    textGroup.append("text").text(function(d) {
      return d.event;
    });
    updateVis();
    events.style("cursor", "pointer");
    events.each(function() {
      return $(this).qtip({
        content: true,
        position: {
          my: "left bottom",
          at: "right center",
          target: "mouse",
          adjust: {
            x: 5,
            y: 0
          }
        },
        style: {
          classes: 'qtip-light'
        }
      });
    });
    events.on("click", function(d) {
      if (!!d.url) {
        return window.open(d.url);
      }
    });
    d3.select("#sortMode").selectAll("a").on("click", function(d) {
      sortingChanged = true;
      sortMode = d3.select(this).attr("data-value");
      return router.navigate("" + sortMode + "/" + scaling, {
        trigger: true
      });
    });
    d3.select("#scaling").selectAll("a").on("click", function(d) {
      sortingChanged = false;
      scaling = d3.select(this).attr("data-value");
      return router.navigate("" + sortMode + "/" + scaling, {
        trigger: true
      });
    });
    return _this;
  };

  $(function() {
    router = new Router();
    _this.eventsData = new Events();
    _this.eventsData.fetch({
      success: function() {
        initVis();
        return Backbone.history.start();
      }
    });
    return $("a").each(function() {
      var _ref;
      if (((_ref = $(this).attr("href")) != null ? _ref.indexOf("#") : void 0) === 0) {
        return $(this).click(function() {
          return $("html, body").animate({
            scrollTop: "0px"
          });
        });
      }
    });
  });

}).call(this);
